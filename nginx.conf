events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Caching Zone Configuration
    proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=admin_cache:10m max_size=1g inactive=60m use_temp_path=off;

    upstream backend_servers {
        # Using least_conn for better load distribution
        least_conn;
        server productionbackend-3sr4.onrender.com:443;
        server productionbackend-o3tk.onrender.com:443;
    }

    server {
        listen 80;
        server_name localhost;

        # Serve static frontend files
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # Proxy API requests to backend
        location /api {
            proxy_pass https://backend_servers;
            
            # Critical for Render/Cloud targeting
            proxy_ssl_server_name on;
            proxy_set_header Host productionbackend-3sr4.onrender.com;
            
            # Forwarding headers
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Handle potential 502/504 by retrying the other server
            proxy_next_upstream error timeout invalid_header http_502 http_503 http_504;

            # Special caching for admin data
            location /api/admin/ {
                proxy_cache admin_cache;
                proxy_cache_valid 200 5m;
                proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
                proxy_cache_lock on;
                proxy_pass https://backend_servers;
            }
        }
    }
}
